node_modules/
dist/
.DS_Store
.env

# Obsidian plugin build artifacts — distributed via GitHub Releases only
# (see .github/workflows/release.yml). Do not commit main.js to the repo.
main.js
main.js.map
